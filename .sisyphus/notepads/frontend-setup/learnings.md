# Learnings

## index.html noscript fallback
- Added `<noscript>` block after `<div id="root">` and before `<script>` tag
- Style uses inline CSS with dark-theme-compatible padding and sans-serif font
- grep count of "noscript" = 2 confirms both opening and closing tags present
